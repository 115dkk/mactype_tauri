name: "CodeQL"

on:
  schedule:
    - cron: "0 4 * * 1"
  workflow_dispatch:

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ matrix.os }}
    permissions:
      security-events: write
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: c-cpp
            os: windows-latest
            build-mode: manual

          - language: javascript-typescript
            os: ubuntu-latest
            build-mode: none

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-extended

      - if: matrix.build-mode == 'manual'
        name: Build C++ (MSVC)
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64
          msbuild gdipp.sln /p:Configuration=Release /p:Platform=x64

      - if: matrix.language == 'c-cpp'
        name: Build service injector x86/x64 for CodeQL
        shell: pwsh
        run: |
          cmake -S service-injector -B build/service-injector-codeql-x86 -A Win32 -DBUILD_TESTING=OFF
          cmake --build build/service-injector-codeql-x86 --config Release --target mactype-injector32
          cmake -S service-injector -B build/service-injector-codeql-x64 -A x64 -DBUILD_TESTING=OFF
          cmake --build build/service-injector-codeql-x64 --config Release --target mactype-injector64

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
