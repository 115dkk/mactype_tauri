{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the main control center window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
